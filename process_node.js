/* This function can be passed as a callback to either visit_nodes function. The
   node parameter is a DOM Node. For an example, uncomment the line to log node
   names to the Javascript console, but be aware that benchmarking this will
   create a lot of output! */
function process_node(node) {
    //console.log(node.nodeName);
}
